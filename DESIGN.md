# Design

이 문서는 사이트의 시각 시스템을 기록한다. 값의 단일 출처는
[`public/css/tokens.css`](public/css/tokens.css)이고, 이 문서는 **왜 그 값인지**를 남긴다.
둘이 어긋나면 문서가 거짓이 되므로 함께 고친다.

## 방향

이 블로그는 하네스, ADR, 코딩 에이전트를 다룬다. 그래서 잡지가 아니라 **작업 표면**처럼
보이는 쪽을 택했다. 왼쪽에 조용한 사이드바가 붙어 글 사이를 이동하고, 오른쪽 한 컬럼에서
읽는다. 장식은 거의 없고, 시선을 끄는 요소는 하나만 둔다.

라이트/다크 두 테마를 지원한다. 시맨틱 토큰 이름(`--color-surface` 등)은 두 테마에서
같고 값만 달라지므로, 개별 스타일시트는 테마를 몰라도 된다.

## 파일 구조

| 파일 | 역할 |
| --- | --- |
| `public/css/tokens.css` | 모든 값의 단일 출처. 라이트/다크 2세트 |
| `public/css/base.css` | 문서 기본값과 본문 타이포그래피 |
| `public/css/layout.css` | 사이드바 셸, 본문 컬럼, 토글 상태 |
| `public/css/components.scss` | 글 목록, 포스트 헤더, 태그, 페이지네이션, 관련 글 |
| `public/css/syntax.css` | 코드 하이라이트 (라이트/다크 2세트) |

`tokens.css`가 가장 먼저 로드돼야 나머지의 `var()`가 해석된다 (`_includes/head.html`).

이전에는 Poole/Hyde 테마를 얹어 쓰다가 두 테마의 미디어 쿼리가 충돌해
루트 폰트 크기가 `48em`에서 되레 작아지는 문제가 있었다. 재작성하면서 그 층을 걷어냈다.

## 서체

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--font-sans` | Pretendard Variable | 본문 전체 |
| `--font-mono` | JetBrains Mono | 날짜 · 태그 · 섹션 라벨 · 코드 |

**Pretendard를 쓰는 이유는 한글이다.** 이전 본문 서체는 PT Sans였는데 한글 글리프가 없어
한국어 본문이 OS 기본 폰트로 떨어졌다. 글이 전부 한국어인 블로그에서 이건 실제 결함이었다.

**모노를 유틸리티 서체로 쓴다.** 날짜, 태그, 섹션 라벨에 모노를 쓰는 건 장식이 아니다.
이 블로그의 글은 `## 1.` `## 2.`로 번호가 매겨진 순차적 논증이고 주제가 코드와 터미널이다.
날짜에는 `font-variant-numeric: tabular-nums`를 걸어 목록에서 세로로 정렬된다.

### 크기

| 토큰 | 값 | 쓰이는 곳 |
| --- | --- | --- |
| `--font-size-root-sm` | `16px` | 48em 미만 |
| `--font-size-root-lg` | `17px` | 48em 이상 |
| `--font-size-display` | `2.25rem` | 포스트·페이지 제목 |
| `--font-size-h1` | `1.75rem` | `h1` |
| `--font-size-h2` | `1.375rem` | `h2` |
| `--font-size-h3` | `1.125rem` | `h3`, 목록의 글 제목 |
| `--font-size-body` | `1rem` | 본문 |
| `--font-size-sm` | `0.875rem` | 발췌, 표, 각주, 네비 항목 |
| `--font-size-meta` | `0.75rem` | 날짜, 태그, 섹션 라벨 |
| `--font-size-code` | `0.8125rem` | 코드 |

루트 크기가 `16px → 17px` 한 단계만 바뀐다. 이전 구조에서는 네 번 바뀌면서 중간에
작아지기도 했는데, 그건 두 테마를 겹쳐 쓴 결과이지 의도가 아니었다.

### 행간·자간

| 토큰 | 값 | 이유 |
| --- | --- | --- |
| `--line-height` | `1.75` | 한글 본문은 라틴 문자보다 넉넉한 행간이 필요하다 |
| `--line-height-tight` | `1.3` | 제목 |
| `--line-height-code` | `1.6` | 코드 블록 |
| `--tracking-tight` | `-0.02em` | 제목 — 큰 글자에서 자간이 벌어 보이는 것을 보정 |
| `--tracking-meta` | `0.06em` | 모노 대문자 라벨 |

## 색

시맨틱 이름만 쓰고, 값은 테마별로 다르다. 아래 표의 값은 라이트 / 다크 순서다.

### 표면

| 토큰 | 라이트 | 다크 | 쓰이는 곳 |
| --- | --- | --- | --- |
| `--color-surface` | `#ffffff` | `#1a1a18` | 본문 배경 |
| `--color-surface-sidebar` | `#f7f7f5` | `#141413` | 사이드바 배경 |
| `--color-surface-subtle` | `#f4f4f2` | `#232320` | 인라인 코드, 태그 |
| `--color-surface-hover` | `#ececea` | `#2b2b27` | 네비 항목 hover |
| `--color-surface-active` | `#e4e4e1` | `#34342f` | 네비 항목 현재 위치 |
| `--color-code-bg` | `#f7f7f5` | `#1f1f1c` | 코드 블록 |

라이트에서는 사이드바가 본문보다 어둡고, 다크에서는 사이드바가 본문보다 더 어둡다.
두 테마에서 "사이드바가 뒤로 물러난다"는 위계가 같게 유지된다.

### 텍스트

| 토큰 | 라이트 | 다크 | 쓰이는 곳 |
| --- | --- | --- | --- |
| `--color-text` | `#35352f` | `#d6d6cf` | 본문 |
| `--color-text-strong` | `#16160f` | `#f2f2ec` | `strong`, hover 상태 |
| `--color-heading` | `#16160f` | `#f2f2ec` | 제목 |
| `--color-text-muted` | `#63635a` | `#b0b0a6` | 발췌, 인용, 표 헤더, 각주 |
| `--color-text-faint` | `#6e6e65` | `#a1a198` | 날짜, 섹션 라벨, 태그라인 |

**muted/faint 값은 대비에서 나왔다.** 처음에 라이트 `#85857c`/`#a3a39a`,
다크 `#8f8f86`/`#6e6e66`으로 잡았더니 각각 2.4~3.7:1이었다. 날짜와 라벨을 모노로
두드러지게 해놓고 읽히지 않으면 의미가 없어서, 두 테마 모두 **본문·사이드바 배경 대비
4.5:1 이상**이 되도록 올렸다. 값을 바꿀 때 이 하한을 먼저 확인한다.

### 링크·강조

| 토큰 | 라이트 | 다크 | 쓰이는 곳 |
| --- | --- | --- | --- |
| `--color-link` | `#1f6f5c` | `#6fc0a6` | 링크, 포커스 링, 아바타 배경 |
| `--color-link-hover` | `#175647` | `#8ed3bc` | 링크 hover |
| `--color-code` | `#b0533f` | `#e0937d` | 인라인 코드 텍스트 |

본문 링크는 기본 상태에 밑줄을 두지 않고 hover에서만 켠다. 한글은 받침이 밑줄에
닿아서 밑줄이 많으면 읽기 어려워진다.

### 경계

| 토큰 | 라이트 | 다크 | 쓰이는 곳 |
| --- | --- | --- | --- |
| `--color-border` | `#e2e2dd` | `#302f2b` | 표 셀, `hr`, 관련 글 구분선 |
| `--color-border-subtle` | `#ececE7` | `#262622` | 목록 항목, 사이드바 우측선, 코드 블록 |
| `--color-border-strong` | `#d3d3cc` | `#3d3c37` | 표 헤더 하단, 인용문 왼쪽선, 스크롤바 |

## 레이아웃

| 토큰 | 값 | 쓰이는 곳 |
| --- | --- | --- |
| `--sidebar-width` | `16rem` | 사이드바 폭 |
| `--content-offset` | `16rem` | 본문 `margin-left` (= 사이드바 폭) |
| `--content-max-width` | `42rem` | 읽기 컬럼 최대 폭 |

**본문은 `margin-left`로 사이드바를 피한다.** 처음에 `padding-left`로 밀었더니
미디어 쿼리마다 offset을 다시 더해야 해서, 뒤에 오는 쿼리가 앞의 값을 덮으며 본문이
사이드바 아래로 깔리는 버그가 났다. margin으로 바꾸고 읽기 폭은 `.content-inner`가
안쪽에서 잡는다. 그래서 `--content-offset`이 `--sidebar-width`와 같은 값이면 된다.

### 사이드바 구조

```
┌────────────────────┐
│ Haandol      ☾ ▣  │  sidebar-head — 워드마크 + 아이콘 버튼
│ TL;DR              │
├────────────────────┤
│ ▬ Home             │  sidebar-body — 알약형 네비
│   About            │
│                    │
│ 지난 글            │  sidebar-label — 모노 대문자
│   ALPS와 ADR로...  │  최근 8개, 2줄까지
│   ...              │
└────────────────────┘
```

사이드바는 글 사이를 이동하는 일만 한다. 프로필 행과 외부 링크(Q&A)를 두지 않고,
저자 정보는 About 페이지가 담당한다.

네비 항목은 `layout: page`인 페이지에서 자동으로 나온다. 그래서 **저장소 문서는
`_config.yml`의 `exclude`에 넣어야 한다** — `github-pages`의 기본 테마가 front matter
없는 `.md`에도 `layout: page`와 H1 기반 `title`을 붙여서, 빼두지 않으면 이 문서
(`DESIGN.md`)까지 네비에 페이지로 올라온다.

현재 위치는 `aria-current` 속성으로 표시하고 CSS가 그걸 선택자로 쓴다
(`.nav-item[aria-current]`). 클래스를 따로 두지 않아 마크업과 스타일이 어긋날 수 없다.

### 토글 상태

| 상태 | 표현 | 저장 |
| --- | --- | --- |
| 데스크톱 사이드바 | `<html class="sidebar-hidden">` (기본 열림) | `localStorage.sidebarHidden` |
| 모바일 사이드바 | `<html class="sidebar-open">` (기본 닫힘) | 저장하지 않음 |
| 테마 | `<html data-theme="dark">` | `localStorage.theme` |

**데스크톱과 모바일이 다른 클래스를 쓴다.** 한 클래스로 합치면 한쪽의 기본값이
다른 쪽에서 뒤집힌다 — 데스크톱은 열림이 기본이고 모바일은 닫힘이 기본이기 때문이다.

**다시 열기 버튼은 사이드바의 형제여야 한다.** 사이드바는 `transform: translateX(-100%)`로
밀려나는데, transform이 걸린 조상 안에서는 `position: fixed` 자식도 함께 이동한다.
자식으로 두면 접은 뒤 버튼을 찾을 수 없다.

**테마와 사이드바 상태는 페인트 전에 복원한다** (`_includes/head.html`의 인라인 스크립트).
`DOMContentLoaded`를 기다리면 라이트 화면이 한 번 번쩍인다. 저장된 테마가 없으면
`prefers-color-scheme`를 따른다.

## 간격

| 토큰 | 값 |
| --- | --- |
| `--space-xxs` | `.25rem` |
| `--space-xs` | `.5rem` |
| `--space-sm` | `.75rem` |
| `--space-md` | `1rem` |
| `--space-lg` | `1.5rem` |
| `--space-xl` | `2rem` |
| `--space-2xl` | `3rem` |
| `--space-3xl` | `4rem` |

### em 기반 간격

인라인 코드 패딩만 `em`이다. `code`는 글자 크기가 본문보다 작아서 `rem`으로 바꾸면
박스가 글자에 비해 커진다. 실제로 토큰화 중에 `rem`으로 바꿨다가 회귀를 냈다.

| 토큰 | 값 | 쓰이는 곳 |
| --- | --- | --- |
| `--space-code-y` | `.2em` | 인라인 `code` 상하 |
| `--space-code-x` | `.4em` | 인라인 `code` 좌우 |

## 모서리·전환

| 토큰 | 값 | 쓰이는 곳 |
| --- | --- | --- |
| `--radius-sm` | `4px` | 인라인 코드, 태그, 아이콘 버튼 |
| `--radius-md` | `8px` | 네비 항목, 코드 블록, 이미지 |
| `--radius-lg` | `12px` | — |
| `--radius-pill` | `999px` | 아바타, 스크롤바 |
| `--transition` | `.18s ease` | hover, 색 |
| `--transition-slow` | `.24s ease` | 사이드바 슬라이드, 테마 전환 |

`prefers-reduced-motion: reduce`에서 모든 전환이 사실상 0이 된다 (`layout.css` 마지막).

## 브레이크포인트

CSS custom property는 미디어 쿼리 조건에 쓸 수 없다. 따라서 아래는 **토큰이 아니라 규약**이고
각 스타일시트에 리터럴로 남아 있다.

| 폭 | 경계에서 바뀌는 것 |
| --- | --- |
| `48em` | 사이드바가 오버레이 → 좌측 고정. 본문이 밀려남. 루트 폰트 16px → 17px |

경계가 하나뿐이라 유지 지점도 하나다.

## Mermaid 다이어그램

Mermaid는 흰 배경을 전제로 렌더링하므로 다크 테마에서 그대로 두면 읽을 수 없다.
그래서 `.mermaid`를 **항상 흰 패널에 얹는다** (`base.css`). 테마별로 다이어그램을
두 벌 렌더하는 것보다 단순하고, 다이어그램이 문서 안의 삽화처럼 보인다.

## 댓글 위젯 (utteranc.es)

댓글은 cross-origin iframe이라 CSS가 닿지 않는다. `_includes/comments.html`이
두 갈래로 처리한다.

1. **최초 로드** — 현재 테마에 맞는 `theme` 속성으로 script를 주입한다.
2. **이후 토글** — iframe에 `postMessage({type:'set-theme'})`를 보낸다.

`<html data-theme>` 변화를 `MutationObserver`로 감시하므로 토글 버튼이 어디에 있는지
알 필요가 없다. 위젯은 비동기로 붙기 때문에 **iframe이 나타난 시점에 현재 테마를 한 번
더 밀어준다** — 그 사이에 사용자가 테마를 바꿨으면 그 변경이 유실되기 때문이다.

## syntax.css

코드 하이라이트는 별도 토큰(`--syn-*`)을 쓴다. 사이트 시맨틱 색과 공유하지 않는다 —
코드 토큰의 색은 의미가 다르고, 테마를 교체할 때 이 파일만 통째로 바꾸는 게 낫다.

셀렉터는 한 벌만 두고 값만 라이트/다크로 갈린다. 이전에는 60여 개 셀렉터가 각자
리터럴 색을 갖고 있어 다크 대응이 불가능했다.

## 값을 바꿀 때

1. `DESIGN.md`(이 문서)와 `tokens.css`를 함께 고친다.
2. 스타일시트에는 리터럴을 넣지 않는다. 새 값이 필요하면 토큰을 추가한다.
3. 색을 바꾸면 **두 테마 모두** 대비를 확인한다. 본문·사이드바 배경 대비 4.5:1이 하한이다.
4. `48em` 경계 양쪽(예: 1440px, 390px)에서 렌더링을 확인한다. 모바일은 사이드바가
   오버레이라 동작이 다르다.
5. 사이드바를 접은 상태와 펼친 상태를 모두 본다.
